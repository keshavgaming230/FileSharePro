services:
  - type: web
    name: fileshare-pro
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python shareit_clone.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PORT
        value: 10000
